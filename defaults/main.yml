---
# defaults file for ansible-role-dsu

# redefine to True if you want to enable Dell hardware check
dsu_assert: False

# url to DSU bootstrap file
dsu-url: "https://linux.dell.com/repo/hardware/dsu/bootstrap.cgi"

# local copy
dsu-local-file: "/root/bootstrap.cgi"
